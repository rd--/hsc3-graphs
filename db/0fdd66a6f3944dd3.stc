// RCD ; ... and off
var freqs = 0.to(7).collect({ arg i; (i + 1) * 100 });
var decays = 0.to(7).collect({ arg i; 1 / (i + 1) });
var clock = LFPulse(8, 0, 0.001);
var rotate = 7;
var spread = 1;
var pulses = RCD(clock, rotate, 0, 0, spread, 0, 0, 0, 0);
var out = Ringz(pulses, freqs, decays) * 0.05;
Splay2(out)
